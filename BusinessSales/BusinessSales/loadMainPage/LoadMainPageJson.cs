namespace BusinessSales
{
    public record LoadMainPageJson(string name, double totalIncome, 
        int totalCount, int avPercent, int countStore, 
        double costStore, string currentDate);
}
